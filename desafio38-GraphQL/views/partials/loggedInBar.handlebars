<section class="row pt-4">
    <div class="card shadow">
        <div class="card-body">
            <div class="row justify-content-between">
                <div class="col-2">
                    <img src="{{userInfo.avatarUrl}}" class="d-inline rounded img-fluid">
                </div>
                <div class="d-flex col-10 justify-content-between align-content-center flex-wrap">
                    <h2 class="d-inline">Bienvenido {{userInfo.first_name}}</h2>
                    <button class="btn btn-primary" id="logOutButton">
                        <a href="/logout" class="text-white text-decoration-none">
                        Logout
                        </a>
                    </button>
                </div>
                <div class="col-1 col-md-3">
                    
                </div>
            </div>
        </div>
    </div>
</section>